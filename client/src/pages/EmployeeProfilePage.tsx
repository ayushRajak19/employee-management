import { useState } from "react"; import { useQuery } from "@tanstack/react-query"; import { ArrowLeft, BadgeCheck, BriefcaseBusiness, CalendarDays, Target, UserRound } from "lucide-react"; import { useNavigate, useParams } from "react-router-dom"; import { employeeApi } from "@/features/employees/employeeApi"; import { EmployeeLifecycleActions } from "@/features/employees/EmployeeLifecycleActions"; import { Skeleton } from "@/components/ui/Skeleton";
const tabs = ["Overview","Skills","Tasks","Projects","Goals","KPIs","Performance","Learning","Documents","Activity"] as const; type Tab = typeof tabs[number];
export const EmployeeProfilePage = () => { const { id = "" } = useParams(); const navigate = useNavigate(); const [tab, setTab] = useState<Tab>("Overview"); const query = useQuery({ queryKey: ["employee", id, "360"], queryFn: () => employeeApi.profile(id), enabled: !!id }); if (query.isLoading) return <main className="p-8"><Skeleton className="h-56"/></main>; if (!query.data) return <main className="grid min-h-96 place-items-center">Employee profile unavailable.</main>; const data = query.data; const employee = data.employee; const collections: Record<Exclude<Tab,"Overview">, unknown[]> = { Skills: data.skills, Tasks: data.tasks, Projects: data.projects, Goals: data.goals, KPIs: data.kpis, Performance: data.performance, Learning: data.training, Documents: data.documents, Activity: data.timeline }; return <main className="flex-1 px-5 py-8 sm:px-8"><div className="mx-auto max-w-[1440px]"><button className="flex items-center gap-2 text-sm text-slate-500 hover:text-ink" onClick={() => navigate("/employees")}><ArrowLeft size={16}/> Employees</button><section className="mt-5 rounded-3xl border bg-white p-6 shadow-soft sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid size-20 place-items-center rounded-3xl bg-brand-50 text-2xl font-semibold text-brand-700">{employee.firstName[0]}{employee.lastName[0]}</div><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-semibold">{employee.firstName} {employee.lastName}</h1><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-semibold text-emerald-700">{employee.status}</span></div><p className="mt-1 text-sm text-slate-500">{employee.employeeId} · {employee.designation.name}</p><div className="mt-4 flex flex-wrap gap-x-6 gap-y-2 text-xs text-slate-500"><span>{employee.department.name}{employee.team && ` / ${employee.team.name}`}</span><span>{employee.employmentType.replaceAll("_", " ")}</span><span>Joined {new Date(employee.dateOfJoining).toLocaleDateString()}</span></div></div><EmployeeLifecycleActions employeeId={employee._id}/><div className="sm:text-right"><p className="text-xs text-slate-400">Profile completion</p><p className="text-2xl font-semibold text-brand-700">{employee.profileCompletion}%</p></div></div></section><div className="mt-5 flex gap-1 overflow-x-auto rounded-xl border bg-white p-1">{tabs.map((item) => <button className={`h-10 whitespace-nowrap rounded-lg px-3 text-sm ${tab === item ? "bg-ink text-white" : "text-slate-500 hover:bg-slate-50"}`} key={item} onClick={() => setTab(item)}>{item}</button>)}</div>{tab === "Overview" ? <div className="mt-5 grid gap-5 lg:grid-cols-3"><section className="rounded-2xl border bg-white p-5 shadow-soft lg:col-span-2"><h2 className="font-semibold">Professional overview</h2><p className="mt-3 text-sm leading-6 text-slate-500">{employee.professionalSummary || "No professional summary has been added."}</p><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{[{ label: "Verified skills", value: data.skills.filter((x) => x.verifiedRating).length, icon: BadgeCheck }, { label: "Open tasks", value: data.tasks.filter((x) => !["COMPLETED","CANCELLED"].includes(x.status)).length, icon: BriefcaseBusiness }, { label: "Active goals", value: data.goals.filter((x) => x.status !== "COMPLETED").length, icon: Target }, { label: "Experience", value: employee.employmentType.replaceAll("_", " "), icon: CalendarDays }].map(({ label,value,icon: Icon }) => <div className="rounded-xl bg-slate-50 p-4" key={label}><Icon size={16} className="text-brand-600"/><p className="mt-3 text-lg font-semibold">{value}</p><p className="text-[11px] text-slate-400">{label}</p></div>)}</div></section><section className="rounded-2xl border bg-white p-5 shadow-soft"><h2 className="font-semibold">Recognition</h2><div className="mt-4 space-y-2">{data.recognition.length ? data.recognition.map((item) => <div className="rounded-xl bg-violet-50 p-3" key={item._id}><p className="text-xs font-semibold text-violet-700">{item.badge.replaceAll("_", " ")}</p><p className="mt-1 text-xs text-violet-600">{item.explanation}</p></div>) : <p className="py-8 text-center text-sm text-slate-400">No recognition yet.</p>}</div></section></div> : <section className="mt-5 rounded-2xl border bg-white shadow-soft"><div className="flex items-center border-b p-5"><UserRound size={17} className="text-brand-600"/><h2 className="ml-2 font-semibold">{tab}</h2><span className="ml-auto text-xs text-slate-400">{collections[tab].length}</span></div><div className="divide-y">{collections[tab].length ? collections[tab].map((raw, index) => { const item = raw as Record<string, unknown>; const skill = item.skill as { name?: string } | undefined; const kpi = item.kpi as { name?: string } | undefined; const training = item.training as { name?: string } | undefined; const title = String(item.name ?? item.title ?? item.originalName ?? skill?.name ?? kpi?.name ?? training?.name ?? item.type ?? `${tab} item`); return <div className="p-5" key={String(item._id ?? index)}><p className="text-sm font-semibold">{title}</p><p className="mt-1 text-xs text-slate-400">{String(item.status ?? item.classification ?? item.verificationStatus ?? item.category ?? item.period ?? "Recorded")}</p>{item.progress !== undefined && <div className="mt-3 h-1.5 rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand-500" style={{ width: `${Number(item.progress)}%` }}/></div>}</div>; }) : <div className="p-12 text-center text-sm text-slate-400">No {tab.toLowerCase()} records yet.</div>}</div></section>}</div></main>; };
