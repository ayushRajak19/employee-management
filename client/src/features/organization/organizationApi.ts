import { api } from "@/api/client"; import type { OrganizationData } from "./types";
export const organizationApi = { list: () => api.get<OrganizationData>("/api/v1/organization"), createDepartment: (body: { name: string; code: string; description?: string }) => api.post("/api/v1/organization/departments", body), createTeam: (body: { name: string; code: string; department: string; description?: string }) => api.post("/api/v1/organization/teams", body), createDesignation: (body: { name: string; code: string; department?: string; level?: string; description?: string; catalogRole: string }) => api.post("/api/v1/organization/designations", body) };
