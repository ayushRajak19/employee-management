import { forwardRef, type ButtonHTMLAttributes } from "react";
import { cn } from "@/lib/cn";
export const Button = forwardRef<HTMLButtonElement, ButtonHTMLAttributes<HTMLButtonElement> & { variant?: "primary" | "secondary" | "ghost" }>(({ className, variant = "primary", ...props }, ref) => <button ref={ref} className={cn("inline-flex h-11 items-center justify-center gap-2 rounded-xl px-4 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-60", variant === "primary" && "bg-brand-600 text-white shadow-sm shadow-brand-200 hover:bg-brand-700", variant === "secondary" && "border border-brand-100 bg-white text-brand-700 hover:bg-brand-50", variant === "ghost" && "text-slate-600 hover:bg-brand-50 hover:text-brand-700", className)} {...props}/>); Button.displayName = "Button";
