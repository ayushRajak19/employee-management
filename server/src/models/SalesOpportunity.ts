import { Schema, type Types } from "mongoose"; import { tenantModel } from "../tenancy/tenantModel.js";
export const OPPORTUNITY_STATUSES = ["OPEN", "WON", "LOST"] as const;
export interface SalesOpportunityDocument { name: string; lead?: Types.ObjectId; customer?: Types.ObjectId; ownerEmployee: Types.ObjectId; territory?: Types.ObjectId; geoNode?: Types.ObjectId; market?: string; stage: string; estimatedValue: number; currency: string; probability: number; expectedCloseDate?: Date; actualCloseDate?: Date; status: typeof OPPORTUNITY_STATUSES[number]; lostReason?: string }
const schema = new Schema<SalesOpportunityDocument>({ name: { type: String, required: true, trim: true, maxlength: 160 }, lead: { type: Schema.Types.ObjectId, ref: "SalesLead" }, customer: { type: Schema.Types.ObjectId, ref: "SalesCustomer", index: true }, ownerEmployee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, territory: { type: Schema.Types.ObjectId, ref: "SalesTerritory", index: true }, geoNode: { type: Schema.Types.ObjectId, ref: "GeoNode", index: true }, market: { type: String, trim: true, maxlength: 120 }, stage: { type: String, required: true, trim: true, maxlength: 80 }, estimatedValue: { type: Number, required: true, min: 0 }, currency: { type: String, uppercase: true, trim: true, maxlength: 3, default: "INR" }, probability: { type: Number, required: true, min: 0, max: 100 }, expectedCloseDate: Date, actualCloseDate: Date, status: { type: String, enum: OPPORTUNITY_STATUSES, default: "OPEN", index: true }, lostReason: { type: String, trim: true, maxlength: 500 } }, { timestamps: true, toJSON: { virtuals: true } });
schema.virtual("weightedValue").get(function () { return this.estimatedValue * this.probability / 100; }); schema.index({ ownerEmployee: 1, status: 1, expectedCloseDate: 1 }); schema.index({ territory: 1, status: 1 }); schema.index({ geoNode: 1, status: 1 });
export const SalesOpportunity = tenantModel<SalesOpportunityDocument>("SalesOpportunity", schema);
