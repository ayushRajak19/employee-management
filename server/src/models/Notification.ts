import { Schema, model, type Types } from "mongoose";
export interface NotificationDocument { recipient: Types.ObjectId; type: string; title: string; body: string; entityType?: string; entityId?: string; readAt?: Date; channels: ("IN_APP" | "EMAIL")[] }
const schema = new Schema<NotificationDocument>({ recipient: { type: Schema.Types.ObjectId, ref: "User", required: true, index: true }, type: { type: String, required: true, index: true }, title: { type: String, required: true, maxlength: 200 }, body: { type: String, required: true, maxlength: 2000 }, entityType: { type: String, maxlength: 80 }, entityId: { type: String, maxlength: 100 }, readAt: Date, channels: { type: [String], enum: ["IN_APP","EMAIL"], default: ["IN_APP"] } }, { timestamps: true }); schema.index({ recipient: 1, readAt: 1, createdAt: -1 }); export const Notification = model<NotificationDocument>("Notification", schema);
