import { Schema, model, type Types } from "mongoose";
export interface EmployeeKPIDocument { employee: Types.ObjectId; kpi: Types.ObjectId; period: string; actual: number; achievement: number; evaluatedBy: Types.ObjectId; comment?: string }
const schema = new Schema<EmployeeKPIDocument>({ employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, kpi: { type: Schema.Types.ObjectId, ref: "KPI", required: true, index: true }, period: { type: String, required: true, index: true }, actual: { type: Number, required: true }, achievement: { type: Number, required: true, min: 0, max: 200 }, evaluatedBy: { type: Schema.Types.ObjectId, ref: "User", required: true }, comment: { type: String, maxlength: 2000 } }, { timestamps: true }); schema.index({ employee: 1, kpi: 1, period: 1 }, { unique: true }); export const EmployeeKPI = model<EmployeeKPIDocument>("EmployeeKPI", schema);
