import { Schema, type Types } from "mongoose";
import { tenantModel } from "../tenancy/tenantModel.js";
export const VERIFICATION_STATUSES = ["PENDING", "VERIFIED", "EXPERT_VERIFIED", "REJECTED", "REVIEW_REQUIRED"] as const;
export interface Evidence { type: "PROJECT" | "GITHUB" | "PORTFOLIO" | "CERTIFICATE" | "DOCUMENT" | "WORK_SAMPLE" | "COMMENT"; url?: string; storageKey?: string; comment?: string }
export interface EmployeeSkillDocument { employee: Types.ObjectId; skill: Types.ObjectId; selfRating: number; yearsOfExperience: number; lastUsed?: Date; description?: string; evidence: Evidence[]; verificationStatus: typeof VERIFICATION_STATUSES[number]; verifiedRating?: number; latestVerification?: Types.ObjectId; isActive: boolean }
const evidenceSchema = new Schema<Evidence>({ type: { type: String, enum: ["PROJECT","GITHUB","PORTFOLIO","CERTIFICATE","DOCUMENT","WORK_SAMPLE","COMMENT"], required: true }, url: { type: String, maxlength: 1000 }, storageKey: { type: String, maxlength: 500 }, comment: { type: String, maxlength: 1000 } }, { _id: true });
const schema = new Schema<EmployeeSkillDocument>({ employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, skill: { type: Schema.Types.ObjectId, ref: "Skill", required: true, index: true }, selfRating: { type: Number, required: true, min: 1, max: 10 }, yearsOfExperience: { type: Number, required: true, min: 0, max: 60 }, lastUsed: Date, description: { type: String, maxlength: 2000 }, evidence: { type: [evidenceSchema], default: [] }, verificationStatus: { type: String, enum: VERIFICATION_STATUSES, default: "PENDING", index: true }, verifiedRating: { type: Number, min: 1, max: 10 }, latestVerification: { type: Schema.Types.ObjectId, ref: "SkillVerification" }, isActive: { type: Boolean, default: true, index: true } }, { timestamps: true });
schema.index({ employee: 1, skill: 1 }, { unique: true }); export const EmployeeSkill = tenantModel<EmployeeSkillDocument>("EmployeeSkill", schema);
