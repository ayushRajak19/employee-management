import { Schema, type Types } from "mongoose";
import { tenantModel } from "../tenancy/tenantModel.js";
export interface EmployeeTimelineDocument { employee: Types.ObjectId; type: string; title: string; description?: string; metadata?: unknown; performedBy?: Types.ObjectId; occurredAt: Date }
const schema = new Schema<EmployeeTimelineDocument>({ employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, type: { type: String, required: true, index: true }, title: { type: String, required: true, maxlength: 200 }, description: { type: String, maxlength: 2000 }, metadata: Schema.Types.Mixed, performedBy: { type: Schema.Types.ObjectId, ref: "User" }, occurredAt: { type: Date, default: Date.now, index: true } }, { timestamps: true, versionKey: false }); schema.index({ employee: 1, occurredAt: -1 }); export const EmployeeTimeline = tenantModel<EmployeeTimelineDocument>("EmployeeTimeline", schema);
