import { tenantModel } from "../tenancy/tenantModel.js";
import { Schema, type Types } from "mongoose"; export const DIFFICULTIES = ["BEGINNER","INTERMEDIATE","ADVANCED","EXPERT"] as const;
export interface AssessmentDocument { name: string; skill: Types.ObjectId; difficulty: typeof DIFFICULTIES[number]; maximumScore: number; passingScore: number; timeLimitMinutes: number; assignedEmployee: Types.ObjectId; assignedBy: Types.ObjectId; attemptDate?: Date; score?: number; result?: "PENDING" | "PASSED" | "FAILED" }
const schema = new Schema<AssessmentDocument>({ name: { type: String, required: true, maxlength: 160 }, skill: { type: Schema.Types.ObjectId, ref: "Skill", required: true, index: true }, difficulty: { type: String, enum: DIFFICULTIES, required: true }, maximumScore: { type: Number, required: true, min: 1 }, passingScore: { type: Number, required: true, min: 0 }, timeLimitMinutes: { type: Number, required: true, min: 1, max: 1440 }, assignedEmployee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, assignedBy: { type: Schema.Types.ObjectId, ref: "User", required: true }, attemptDate: Date, score: { type: Number, min: 0 }, result: { type: String, enum: ["PENDING","PASSED","FAILED"], default: "PENDING", index: true } }, { timestamps: true });
export const Assessment = tenantModel<AssessmentDocument>("Assessment", schema);
