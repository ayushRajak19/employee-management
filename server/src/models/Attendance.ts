import { Schema, model, type Types } from "mongoose"; export interface AttendanceDocument { employee: Types.ObjectId; date: Date; checkIn?: Date; checkOut?: Date; workMode?: string; shift?: string; status: string }
const schema = new Schema<AttendanceDocument>({ employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, date: { type: Date, required: true, index: true }, checkIn: Date, checkOut: Date, workMode: { type: String, maxlength: 40 }, shift: { type: String, maxlength: 80 }, status: { type: String, required: true, maxlength: 40 } }, { timestamps: true }); schema.index({ employee: 1, date: 1 }, { unique: true }); export const Attendance = model<AttendanceDocument>("Attendance", schema);
