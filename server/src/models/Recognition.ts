import { tenantModel } from "../tenancy/tenantModel.js";
import { Schema, type Types } from "mongoose"; export const BADGES = ["TOP_PERFORMER","QUALITY_CHAMPION","SKILL_MASTER","FAST_LEARNER","ON_TIME_DELIVERY","CUSTOMER_CHAMPION","MENTOR"] as const;
export interface RecognitionDocument { employee: Types.ObjectId; badge: typeof BADGES[number]; explanation: string; evidence: string[]; awardedBy: Types.ObjectId; awardedAt: Date }
const schema = new Schema<RecognitionDocument>({ employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, badge: { type: String, enum: BADGES, required: true, index: true }, explanation: { type: String, required: true, maxlength: 1000 }, evidence: [{ type: String, maxlength: 500 }], awardedBy: { type: Schema.Types.ObjectId, ref: "User", required: true }, awardedAt: { type: Date, default: Date.now } }, { timestamps: true }); export const Recognition = tenantModel<RecognitionDocument>("Recognition", schema);
