import { Schema } from "mongoose"; import { tenantModel } from "../tenancy/tenantModel.js";
export interface SalesConfigurationDocument { key: "DEFAULT"; defaultLeadCapacityPerEmployee: number; responseSlaMinutes: number; healthyConversionRate: number; opportunityWeights: { demand: number; coverageGap: number; customerWhiteSpace: number; pipelinePotential: number; growth: number; conversionPotential: number } }
const schema = new Schema<SalesConfigurationDocument>({ key: { type: String, enum: ["DEFAULT"], default: "DEFAULT", unique: true }, defaultLeadCapacityPerEmployee: { type: Number, min: 1, default: 300 }, responseSlaMinutes: { type: Number, min: 1, default: 240 }, healthyConversionRate: { type: Number, min: 0, max: 100, default: 15 }, opportunityWeights: { demand: { type: Number, min: 0, default: 20 }, coverageGap: { type: Number, min: 0, default: 25 }, customerWhiteSpace: { type: Number, min: 0, default: 15 }, pipelinePotential: { type: Number, min: 0, default: 15 }, growth: { type: Number, min: 0, default: 10 }, conversionPotential: { type: Number, min: 0, default: 15 } } }, { timestamps: true });
export const SalesConfiguration = tenantModel<SalesConfigurationDocument>("SalesConfiguration", schema);
