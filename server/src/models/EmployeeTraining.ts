import { Schema, model, type Types } from "mongoose"; import { TRAINING_STATUSES } from "./Training.js";
export interface EmployeeTrainingDocument { training: Types.ObjectId; employee: Types.ObjectId; status: typeof TRAINING_STATUSES[number]; assignedBy: Types.ObjectId; certificateStorageKey?: string; result?: string; completedAt?: Date }
const schema = new Schema<EmployeeTrainingDocument>({ training: { type: Schema.Types.ObjectId, ref: "Training", required: true, index: true }, employee: { type: Schema.Types.ObjectId, ref: "Employee", required: true, index: true }, status: { type: String, enum: TRAINING_STATUSES, default: "ASSIGNED", index: true }, assignedBy: { type: Schema.Types.ObjectId, ref: "User", required: true }, certificateStorageKey: { type: String, maxlength: 500 }, result: { type: String, maxlength: 1000 }, completedAt: Date }, { timestamps: true }); schema.index({ training: 1, employee: 1 }, { unique: true }); export const EmployeeTraining = model<EmployeeTrainingDocument>("EmployeeTraining", schema);
