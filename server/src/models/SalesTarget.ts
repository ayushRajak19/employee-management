import { Schema, type Types } from "mongoose"; import { tenantModel } from "../tenancy/tenantModel.js";
export interface SalesTargetDocument { territory?: Types.ObjectId; employee?: Types.ObjectId; periodType: "MONTHLY" | "QUARTERLY" | "YEARLY"; periodStart: Date; periodEnd: Date; revenueTarget: number; currency: string; leadTarget?: number; conversionTarget?: number; customerAcquisitionTarget?: number; status: "DRAFT" | "ACTIVE" | "CLOSED"; justification: string; assignedBy?: Types.ObjectId }
const schema = new Schema<SalesTargetDocument>({ territory: { type: Schema.Types.ObjectId, ref: "SalesTerritory", index: true }, employee: { type: Schema.Types.ObjectId, ref: "Employee", index: true }, periodType: { type: String, enum: ["MONTHLY", "QUARTERLY", "YEARLY"], required: true }, periodStart: { type: Date, required: true, index: true }, periodEnd: { type: Date, required: true }, revenueTarget: { type: Number, required: true, min: 0 }, currency: { type: String, uppercase: true, trim: true, maxlength: 3, required: true }, leadTarget: { type: Number, min: 0 }, conversionTarget: { type: Number, min: 0, max: 100 }, customerAcquisitionTarget: { type: Number, min: 0 }, status: { type: String, enum: ["DRAFT", "ACTIVE", "CLOSED"], default: "ACTIVE", index: true } }, { timestamps: true });
schema.pre("validate", function () { if (!this.employee && !this.territory) this.invalidate("employee", "Target requires an employee or territory"); if (this.periodEnd < this.periodStart) this.invalidate("periodEnd", "Period end must be after period start"); }); schema.index({ employee: 1, periodStart: 1, periodEnd: 1 }); schema.index({ territory: 1, periodStart: 1, periodEnd: 1 });
export const SalesTarget = tenantModel<SalesTargetDocument>("SalesTarget", schema);
