import { Router } from "express"; import * as controller from "../controllers/workController.js"; import { authenticate, requirePermission } from "../middleware/auth.js"; import { validate } from "../middleware/validate.js"; import { asyncHandler } from "../utils/asyncHandler.js"; import { manualTaskSchema, projectSchema, taskReviewSchema, taskSchema, taskTransitionSchema } from "../validators/workValidators.js";
export const workRouter = Router(); workRouter.use(authenticate);
workRouter.get("/projects", asyncHandler(controller.projects)); workRouter.post("/projects", requirePermission("task.create"), validate(projectSchema), asyncHandler(controller.createProject));
workRouter.get("/tasks/metrics", asyncHandler(controller.metrics)); workRouter.get("/tasks", asyncHandler(controller.tasks)); workRouter.post("/tasks", requirePermission("task.create", "task.assign"), validate(taskSchema), asyncHandler(controller.createTask));
workRouter.post("/tasks/manual", requirePermission("task.update"), validate(manualTaskSchema), asyncHandler(controller.createManualTask));
workRouter.patch("/tasks/:id/status", requirePermission("task.update"), validate(taskTransitionSchema), asyncHandler(controller.transition)); workRouter.patch("/tasks/:id/review", requirePermission("task.review"), validate(taskReviewSchema), asyncHandler(controller.review)); workRouter.get("/tasks/:id/activity", asyncHandler(controller.activity));
