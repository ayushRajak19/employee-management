import type { Request, Response } from "express"; import * as service from "../services/workService.js";
import * as leadWork from "../services/leadWorkService.js";
export const projects = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Projects retrieved", data: { items: await service.listProjects({ id: request.user!.id, role: request.user!.role }) } }); };
export const createProject = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Project created", data: { item: await service.createProject(request.body, request.user!.id) } }); };
export const tasks = async (request: Request, response: Response): Promise<void> => { const items=await service.listTasks(request.query as { status?: string; project?: string; priority?: string }, { id: request.user!.id, role: request.user!.role }); response.json({ success: true, message: "Tasks retrieved", data: { items: await leadWork.markLeadTasks(items) } }); };
export const createTask = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Task created", data: { item: await service.createTask(request.body, request.user!.id) } }); };
export const createManualTask = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Manual task added", data: { item: await service.createManualTask(request.body, { id: request.user!.id, role: request.user!.role }) } }); };
export const transition = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Task status updated", data: { item: await service.transitionTask(String(request.params.id), request.body, { id: request.user!.id, role: request.user!.role }) } }); };
export const review = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Task review recorded", data: { item: await service.reviewTask(String(request.params.id), request.body, { id: request.user!.id, role: request.user!.role }) } }); };
export const reassign = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Task reassigned", data: { item: await service.reassignTask(String(request.params.id), request.body.assignedEmployee, { id: request.user!.id, role: request.user!.role }) } }); };
export const remove = async (request: Request, response: Response): Promise<void> => { await service.archiveTask(String(request.params.id), { id: request.user!.id, role: request.user!.role }); response.json({ success: true, message: "Task deleted", data: {} }); };
export const activity = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Task activity retrieved", data: { items: await service.taskActivity(String(request.params.id)) } }); };
export const metrics = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Task metrics retrieved", data: await service.taskMetrics({ id: request.user!.id, role: request.user!.role }) }); };
export const gamification = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Gamification profile retrieved", data: await service.getGamificationForUser(request.user!.id) }); };
const actor = (request: Request) => ({ id: request.user!.id, role: request.user!.role });
export const previewLeadImport = async (request: Request, response: Response): Promise<void> => { if (!request.file) throw new Error("Lead file is required"); response.json({ success: true, message: "Lead file parsed", data: await leadWork.previewLeadFile(request.file, request.body.sheet) }); };
export const commitLeadImport = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Lead list task created", data: await leadWork.commitLeadImport(request.body, actor(request)) }); };
export const leadWorkItems = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Lead work items retrieved", data: await leadWork.listWorkItems(String(request.params.id), request.query, actor(request)) }); };
export const leadWorkItem = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Lead work item retrieved", data: await leadWork.workItemDetail(String(request.params.id), String(request.params.workItemId), actor(request)) }); };
export const logLeadActivity = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Lead activity logged", data: { item: await leadWork.addActivity(String(request.params.id), String(request.params.workItemId), request.body, actor(request)) } }); };
export const leadWorkSummary = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Lead task summary retrieved", data: await leadWork.leadSummary(String(request.params.id), actor(request)) }); };
export const exportLeadWork = async (request: Request, response: Response): Promise<void> => { const csv=await leadWork.exportWorkItems(String(request.params.id),actor(request)); response.setHeader("Content-Type","text/csv; charset=utf-8"); response.setHeader("Content-Disposition",`attachment; filename="lead-task-${request.params.id}.csv"`); response.send(csv); };
