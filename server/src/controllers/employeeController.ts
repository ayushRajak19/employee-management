import type { Request, Response } from "express"; import * as service from "../services/employeeService.js"; import { AppError } from "../utils/AppError.js";
export const create = async (request: Request, response: Response): Promise<void> => { const result = await service.createEmployee(request.body, request.user!.id, { ip: request.ip, userAgent: request.get("user-agent") }); response.status(201).json({ success: true, message: "Employee account created", data: result }); };
export const list = async (request: Request, response: Response): Promise<void> => { const data = await service.listEmployees(request.query as unknown as Parameters<typeof service.listEmployees>[0], { id: request.user!.id, role: request.user!.role }); response.json({ success: true, message: "Employees retrieved", data }); };
export const me = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Employee profile retrieved", data: { employee: await service.getMyEmployee(request.user!.id) } }); };
export const onboarding = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: request.body.complete ? "Onboarding completed" : "Onboarding progress saved", data: { employee: await service.updateOnboarding(request.user!.id, request.body) } }); };
export const updateMe = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Profile updated", data: { employee: await service.updateMyProfile(request.user!.id, request.body) } }); };
export const profilePhoto = async (request: Request, response: Response): Promise<void> => { if (!request.file) throw new AppError("Profile photo is required", 422); response.status(201).json({ success: true, message: "Profile photo updated", data: { employee: await service.updateProfilePhoto(request.user!.id, request.file) } }); };
export const profile = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Employee 360 profile retrieved", data: await service.employee360(String(request.params.id), { id: request.user!.id, role: request.user!.role }) }); };
export const update = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Employee updated", data: { employee: await service.updateEmployee(String(request.params.id), request.body, request.user!.id) } }); };
export const deactivate = async (request: Request, response: Response): Promise<void> => { await service.deactivateEmployee(String(request.params.id), request.user!.id); response.json({ success: true, message: "Employee deactivated", data: {} }); };
