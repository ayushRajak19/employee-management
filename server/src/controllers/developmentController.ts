import type { Request, Response } from "express"; import * as service from "../services/developmentService.js"; const viewer = (request: Request) => ({ id: request.user!.id, role: request.user!.role });
export const training = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Training retrieved", data: await service.listTraining(viewer(request)) }); };
export const createTraining = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Training created", data: { item: await service.createTraining(request.body) } }); };
export const assign = async (request: Request, response: Response): Promise<void> => { response.status(201).json({ success: true, message: "Training assigned", data: { item: await service.assignTraining(String(request.params.id), request.body, request.user!.id) } }); };
export const update = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Training progress updated", data: { item: await service.updateAssignment(String(request.params.id), request.body, viewer(request)) } }); };
export const workload = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Workload retrieved", data: { items: await service.workload(viewer(request)) } }); };
export const matching = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Staffing recommendations calculated", data: { items: await service.matchEmployees(request.body), recommendationOnly: true } }); };
export const growth = async (request: Request, response: Response): Promise<void> => { response.json({ success: true, message: "Skill growth retrieved", data: { items: await service.growth(viewer(request)) } }); };
